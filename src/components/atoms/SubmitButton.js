import React from 'react';

const SubmitButton = () => {
  return (
    <button
      type="submit"
      value="submit"
      className="button is-primary"
    >
      Submit
    </button>
  )
}

export default SubmitButton;
